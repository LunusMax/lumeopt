\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    node distance=0.8cm,
    box/.style={
        rectangle,
        draw,
        rounded corners,
        align=center,
        minimum width=3.2cm,
        minimum height=1.3cm
    },
    arrow/.style={
        -{Latex[length=3mm]},
        thick
    }
]

\node[box] (stats) {Player performance\\statistics\\(season $t-1$)};
\node[box, right=of stats] (pca) {PCA\\47 $\rightarrow$ 20};
\node[box, right=of pca] (team) {Team\\representation};
\node[box, right=of team] (mlp) {MLP};
\node[box, right=of mlp] (pred) {Predicted\\team points};
\node[box, right=of pred] (opt) {Optimization};
\node[box, right=of opt] (rec) {Recruitment\\simulation};

\draw[arrow] (stats) -- (pca);
\draw[arrow] (pca) -- (team);
\draw[arrow] (team) -- (mlp);
\draw[arrow] (mlp) -- (pred);
\draw[arrow] (pred) -- (opt);
\draw[arrow] (opt) -- (rec);

\end{tikzpicture}
\caption{Overview of the proposed framework combining machine learning prediction and squad optimization.}
\label{fig:framework}
\end{figure}