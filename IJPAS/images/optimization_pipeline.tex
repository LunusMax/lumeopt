\begin{tikzpicture}[
    node distance=0.8cm,
    box/.style={
        rectangle,
        draw,
        rounded corners,
        align=center,
        minimum width=5.2cm,
        minimum height=1cm
    },
    smallbox/.style={
        rectangle,
        draw,
        rounded corners,
        align=center,
        minimum width=3.4cm,
        minimum height=1.2cm
    },
    arrow/.style={
        -{Latex[length=3mm]},
        thick
    }
]

\node[box] (squad) {Current squad};
\node[box, below=of squad] (cand) {Candidate recruitments};
\node[box, below=of cand] (budget) {Check synthetic market value budget};

\node[smallbox, below left=1cm and 0.7cm of budget] (greedy) {Greedy search\\(simulate recruitment\\and replace lowest\\playing time players)};

\node[smallbox, below right=1cm and 0.7cm of budget] (local) {Local search\\(swap players and\\replace lowest\\playing time players)};

\node[box, below=2.2cm of budget] (eval) {Evaluate predicted team points (MLP)};
\node[box, below=of eval] (result) {Best feasible recruitment set};

\draw[arrow] (squad) -- (cand);
\draw[arrow] (cand) -- (budget);

\draw[arrow] (budget) -- (greedy);
\draw[arrow] (budget) -- (local);

\draw[arrow] (greedy) -- (eval);
\draw[arrow] (local) -- (eval);

\draw[arrow] (eval) -- (result);

\end{tikzpicture}